# Core Framework
fastapi==0.127.0
uvicorn[standard]==0.40.0
python-dotenv==1.2.1
python-multipart==0.0.21
pydantic==2.12.5
pydantic-settings==2.12.0
httpx==0.28.1
jinja2==3.1.6
websockets==12.0

# AI/ML APIs
# NOTE: anthropic updated for security and API compatibility
# Breaking change: Use client.messages.create() instead of client.completion()
anthropic>=0.50.0
openai==1.109.1

# Telephony
twilio==8.11.0

# Data Storage
redis[hiredis]==5.0.1
aiosqlite==0.22.1
asyncpg==0.31.0  # PostgreSQL async driver

# Week 1: Infrastructure & Resilience
structlog==24.1.0
tenacity==8.2.3
circuitbreaker==1.4.0

# Week 1: Testing
pytest==7.4.4
pytest-asyncio==0.23.4
pytest-cov==4.1.0
fakeredis==2.33.0

# Week 2: Production Hardening
alembic==1.17.2
sqlalchemy[asyncio]==2.0.45
bandit==1.9.2
safety==2.3.5

# Security (Plan A)
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
# Updated cryptography for CVE fixes (was 41.0.7)
cryptography>=42.0.0

# Week 3: Vector Database and Embeddings
chromadb==0.4.22
# PyTorch (CPU version) - Required for sentence-transformers
torch>=2.0.0,<2.2.0
sentence-transformers>=2.2.0,<3.0.0
numpy>=1.24.0
neo4j==5.16.0

# Week 3: WebRTC Support
# aiortc==1.6.0 # Skipped - requires FFmpeg libs

# Week 4: Scale Testing and Production
locust==2.42.6
psutil==5.9.8
ruff==0.14.10
mypy==1.19.1

# Security: XXE Protection
defusedxml>=0.7.1

# Week 4: Multimodal Support
python-magic>=0.4.27  # File type detection via magic bytes
aiofiles>=23.2.1  # Async file operations

# Observability: Log Aggregation (Optional - install as needed)
# DataDog: Install datadog package if using DataDog
# datadog>=0.48.0
# CloudWatch: Install boto3 and watchtower if using AWS CloudWatch
# boto3>=1.34.0
# watchtower>=3.0.0
# ELK: Install CMRESHandler if using Elasticsearch
# CMRESHandler>=1.0.0
